// 页面需要什么状态? 用户信息显示， token 
const initState = {
    showSettings: false,
    sidebarLogo: '',// logo 图片
    fixedHeader: false,
    tagsView: []
}
export default function settings(state = initState, action) {
    switch(action.type) {
        default:
            return state
    }
}